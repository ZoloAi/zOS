"""
zRaven subsystem modules.

Structure:
    utils/          — colors, parser, reporter, data_manager, validator, viewport
    assertions/     — evaluate_assert, dom_assert, style_assert
    cli/            — CLIRunner (zCLI apps via stdin/stdout)
    ws/             — ZRaven (Bifrost apps via WS + Playwright)
    runner.py       — ZRavenRunner (orchestrator, in-process)
    primitives/     — reserved for future zRaven primitive extensions
"""
